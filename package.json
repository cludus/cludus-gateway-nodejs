{
  "name": "cludus-gateway-node",
  "version": "0.0.1",
  "description": "Cludus gateway with NodeJS",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "rimraf ./build",
    "build": "npm run clean && tsc",
    "start": "npm run build && node build/index.js",
    "start:dev": "NODE_ENV=development npx nodemon"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ws": "^8.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.9",
    "@types/ws": "^8.5.5",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
